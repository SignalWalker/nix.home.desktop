{
  ...
}:
{
  options = { };
  disabledModules = [ ];
  imports = [ ];
  config = {
    desktop.keybinds = {
      weatherToggle = {
        modifiers = [ "MOD3" ];
        keysym = "W";
        description = "toggle weather information";
      };
    };
  };
  meta = { };
}
