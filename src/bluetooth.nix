{
  ...
}:
{
  options = { };
  disabledModules = [ ];
  imports = [ ];
  config = {
    desktop.keybinds = {
      bluetoothMenuToggle = {
        modifiers = [ "MOD3" ];
        keysym = "B";
        description = "toggle bluetooth menu";
      };
    };
  };
  meta = { };
}
